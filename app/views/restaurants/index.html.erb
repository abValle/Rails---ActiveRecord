<h1> All Restaurants</h1>

<%= link_to "Create a new Restaurant", new_restaurant_path %>
<%  @restaurants.each do |rest|%>
  <h5> <%= link_to rest.name, restaurant_path(rest) %> -> Rating <%= rest.rating%><h5>
  <p><%= rest.address %></p>
<% end %>
